<!DOCTYPE html>
<html>
	<head>
		<title>Miser</title>
		<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
		<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
		<%= csrf_meta_tags %>
	</head>
	<body class='<%= controller.controller_name %>'>
		<div id="banner">
			<%= image_tag("logo.png")%>
			<%= @page_title || t('.title')%>
		</div>
		<div id="columns">
			<div id="side">
				<ul>
          <li><%= link_to t('.menu_banks'), banks_path %></li>
          <li><%= link_to t('.menu_accounts'), accounts_path %></li>
          <li><%= link_to t('.menu_operations'), operations_path %></li>
          <li><%= link_to t('.menu_users'), users_path %></li>
				</ul>
			</div>
			<div id="main">
				<%= yield %>
			</div>
		</div>
	</body>
</html>
